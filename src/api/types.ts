export type Tokens = {
  access: string;
  refresh: string;
};

export type AllPostsResponseResult = {
  id: number;
  image: string;
  text: string;
  date: string;
  title: string;
  author: number;
};
